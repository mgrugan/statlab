/* StatLab — Section 2: Applied Problems
 * Real-world scenarios. You write a solution in the REQUIRED language; grading
 * checks that the key concepts appear (req regexes run against your code with
 * whitespace stripped and quotes normalized). Reveal shows a model solution.
 * Fields: id, lang, topic, diff, title, scenario, task,
 *         req: [{re, hint}], sol, exp.
 */
export const APPLIED = [

/* ============================ R APPLIED (50) ============================ */

{id:"ar01", lang:"r", topic:"Grouped Summaries", diff:1, title:"Salary by education",
 scenario:"HR gives you a data frame `emp` with numeric `Salary` and factor `Education`.",
 task:"Compute the average salary per education level using base R's apply family.",
 req:[{re:/tapply\(/i, hint:"use tapply()"},
      {re:/emp\$Salary/i, hint:"pass the Salary column as the values"},
      {re:/emp\$Education/i, hint:"group by the Education column"},
      {re:/mean/i, hint:"aggregate with mean"}],
 sol:"tapply(emp$Salary, emp$Education, mean)",
 exp:"tapply(values, INDEX, FUN) is base R's split-apply-combine — exactly the flashcard gradebook pattern."},

{id:"ar02", lang:"r", topic:"Hypothesis Testing", diff:2, title:"Drug trial t-test",
 scenario:"A clinical trial recorded blood-pressure reduction for `drug` and `placebo` groups (numeric vectors).",
 task:"Test whether the mean reduction differs between groups, and store the result in `res`.",
 req:[{re:/res(<-|=)/i, hint:"assign the result to res"},
      {re:/t\.test\(/i, hint:"use t.test()"},
      {re:/drug/i, hint:"include the drug group"},
      {re:/placebo/i, hint:"include the placebo group"}],
 sol:"res <- t.test(drug, placebo)",
 exp:"t.test(a, b) runs Welch's two-sample test; res$p.value and res$conf.int hold the pieces you report."},

{id:"ar03", lang:"r", topic:"dplyr Pipelines", diff:2, title:"Revenue by category",
 scenario:"An e-commerce data frame `orders` has `category`, `price`, and `qty`. Finance wants 2024 revenue per category.",
 task:"With dplyr, create `revenue = price * qty` with mutate, then compute total revenue per category (group + summarize) into a column named `total`.",
 req:[{re:/mutate\(/i, hint:"create the revenue column with mutate()"},
      {re:/revenue=price\*qty/i, hint:"revenue = price * qty"},
      {re:/group_by\(category\)/i, hint:"group_by(category)"},
      {re:/summari[sz]e\(/i, hint:"summarize per group"},
      {re:/total=sum\(revenue\)/i, hint:"total = sum(revenue)"}],
 sol:"orders %>%\n  mutate(revenue = price * qty) %>%\n  group_by(category) %>%\n  summarize(total = sum(revenue))",
 exp:"The mutate → group_by → summarize pipe is the everyday shape of dplyr analysis; pipes never modify the original df."},

{id:"ar04", lang:"r", topic:"Data Cleaning", diff:1, title:"Missing-data audit",
 scenario:"A survey data frame `sv` arrived messy and the team wants a per-column missingness report.",
 task:"Count the number of NA values in each column of `sv` in one line.",
 req:[{re:/colSums\(|sapply\(/i, hint:"colSums() (or sapply) over columns"},
      {re:/is\.na\(/i, hint:"detect missing values with is.na()"}],
 sol:"colSums(is.na(sv))",
 exp:"is.na(sv) gives a logical matrix; colSums() adds TRUEs down each column. Never compare against NA with ==."},

{id:"ar05", lang:"r", topic:"Bootstrap", diff:3, title:"Bootstrap CI for the median",
 scenario:"Household incomes `inc` are heavily skewed, so a normal-theory interval for the median is off the table.",
 task:"Generate 1000 bootstrap medians of `inc` (sample with replacement) into `boots`, then compute the 2.5% and 97.5% quantiles.",
 req:[{re:/replicate\(1000|replicate\(1e3/i, hint:"replicate(1000, ...)"},
      {re:/sample\(inc/i, hint:"resample inc with sample()"},
      {re:/replace=(TRUE|T)/i, hint:"sample WITH replacement"},
      {re:/median\(/i, hint:"take the median of each resample"},
      {re:/quantile\(/i, hint:"finish with quantile(boots, c(.025, .975))"}],
 sol:"boots <- replicate(1000, median(sample(inc, replace = TRUE)))\nquantile(boots, c(0.025, 0.975))",
 exp:"The percentile bootstrap: resample at full size with replacement, recompute the statistic, read CI limits off the empirical quantiles."},

{id:"ar06", lang:"r", topic:"Simulation", diff:2, title:"CLT in action",
 scenario:"You want to show a student that means of skewed data look normal.",
 task:"Simulate 1000 sample means, each from 40 draws of an Exponential(rate = 1), storing them in `m`.",
 req:[{re:/m(<-|=)/i, hint:"assign to m"},
      {re:/replicate\(1000/i, hint:"replicate(1000, ...)"},
      {re:/mean\(/i, hint:"take the mean of each sample"},
      {re:/rexp\(40/i, hint:"draw 40 values with rexp(40)"}],
 sol:"m <- replicate(1000, mean(rexp(40)))",
 exp:"replicate() repeats an expression; hist(m) will look strikingly normal even though rexp() is very skewed — the CLT at work."},

{id:"ar07", lang:"r", topic:"Regression", diff:2, title:"House price model",
 scenario:"A realtor's data frame `homes` has `price`, `sqft`, and `beds`.",
 task:"Fit a linear regression of price on sqft and beds, saving the model as `fit`.",
 req:[{re:/fit(<-|=)/i, hint:"save the model as fit"},
      {re:/lm\(/i, hint:"use lm()"},
      {re:/price~sqft\+beds/i, hint:"formula: price ~ sqft + beds"},
      {re:/data=homes|,homes\)/i, hint:"point data = homes"}],
 sol:"fit <- lm(price ~ sqft + beds, data = homes)",
 exp:"Multiple regression just adds terms with +. summary(fit) reports coefficients, p-values, and R²."},

{id:"ar08", lang:"r", topic:"Regression", diff:2, title:"Price a new listing",
 scenario:"Using the fitted model `fit` from the house-price analysis, a client asks what a 2000 sqft, 3-bed home should cost.",
 task:"Predict the price for that home with `predict()` and a one-row data frame.",
 req:[{re:/predict\(fit/i, hint:"call predict(fit, ...)"},
      {re:/newdata=|data\.frame\(/i, hint:"pass newdata = data.frame(...)"},
      {re:/sqft=2000/i, hint:"sqft = 2000"},
      {re:/beds=3/i, hint:"beds = 3"}],
 sol:"predict(fit, newdata = data.frame(sqft = 2000, beds = 3))",
 exp:"predict(model, newdata) scores new observations; newdata's column names must match the formula's variables."},

{id:"ar09", lang:"r", topic:"Correlation", diff:1, title:"Ad spend vs. sales",
 scenario:"Marketing suspects monthly `ad_spend` drives `sales` (numeric vectors, same length).",
 task:"Compute the Pearson correlation between them.",
 req:[{re:/cor\(/i, hint:"use cor()"},
      {re:/ad_spend/i, hint:"include ad_spend"},
      {re:/sales/i, hint:"include sales"}],
 sol:"cor(ad_spend, sales)",
 exp:"cor(x, y) gives Pearson by default; method = \"spearman\" for a rank-based version robust to outliers."},

{id:"ar10", lang:"r", topic:"Hypothesis Testing", diff:3, title:"Churn vs. plan type",
 scenario:"A telecom data frame `cust` has categorical columns `plan` and `churned`. Do churn rates depend on plan?",
 task:"Run a chi-square test of independence on the two columns (build the contingency table inline).",
 req:[{re:/chisq\.test\(/i, hint:"use chisq.test()"},
      {re:/table\(/i, hint:"build the contingency table with table()"},
      {re:/cust\$plan/i, hint:"cust$plan"},
      {re:/cust\$churned/i, hint:"cust$churned"}],
 sol:"chisq.test(table(cust$plan, cust$churned))",
 exp:"table() cross-tabulates the two factors; chisq.test() then tests independence of rows and columns."},

{id:"ar11", lang:"r", topic:"PCA & Multivariate", diff:3, title:"Compress sensor features",
 scenario:"A factory dataset `sens` has 40 correlated numeric sensor columns; the ML team wants a compact representation.",
 task:"Run PCA with scaling, save as `p`, then display the variance explained via `summary()`.",
 req:[{re:/p(<-|=)prcomp\(/i, hint:"p <- prcomp(...)"},
      {re:/scale\.?=(TRUE|T)/i, hint:"scale the variables (scale = TRUE)"},
      {re:/summary\(p\)/i, hint:"summary(p) shows variance explained"}],
 sol:"p <- prcomp(sens, scale = TRUE)\nsummary(p)",
 exp:"Pick k where the cumulative-variance curve's slope goes from steep to shallow (the elbow) — and remember PCs are uncorrelated, not independent."},

{id:"ar12", lang:"r", topic:"Joins", diff:2, title:"Attach orders to customers",
 scenario:"`customers` (one row per `id`) and `orders` (many rows per `id`) live in separate tables. You need every customer kept, with orders attached where they exist.",
 task:"Left-join `orders` onto `customers` by `id` using dplyr.",
 req:[{re:/left_join\(/i, hint:"use left_join()"},
      {re:/customers/i, hint:"customers is the left table"},
      {re:/orders/i, hint:"orders is the right table"},
      {re:/by="id"/i, hint:'join key: by = "id"'}],
 sol:"left_join(customers, orders, by = \"id\")",
 exp:"A left join keeps all left-table rows, filling NA where no match exists — merge(customers, orders, all.x = TRUE) in base R."},

{id:"ar13", lang:"r", topic:"Reshaping", diff:2, title:"Gradebook to wide",
 scenario:"A long gradebook `gb` has `Student`, `Assignment`, `Score` — one row per student-assignment. The registrar wants one row per student.",
 task:"Widen `gb` so assignments become columns holding the scores (tidyr).",
 req:[{re:/pivot_wider\(/i, hint:"use pivot_wider()"},
      {re:/names_from=Assignment/i, hint:"names_from = Assignment"},
      {re:/values_from=Score/i, hint:"values_from = Score"}],
 sol:"pivot_wider(gb, names_from = Assignment, values_from = Score)",
 exp:"pivot_wider spreads rows into columns keyed by names_from — same operation as pandas' DataFrame.pivot()."},

{id:"ar14", lang:"r", topic:"Reshaping", diff:2, title:"Sensors to long",
 scenario:"`readings` is wide: a `time` column plus one column per sensor (`s1`, `s2`, `s3`). ggplot needs long data.",
 task:"Lengthen `readings`, stacking s1:s3 into `sensor` (names) and `value` (values).",
 req:[{re:/pivot_longer\(/i, hint:"use pivot_longer()"},
      {re:/s1:s3|c\(s1,s2,s3\)|-time/i, hint:"cols = s1:s3"},
      {re:/names_to="sensor"/i, hint:'names_to = "sensor"'},
      {re:/values_to="value"/i, hint:'values_to = "value"'}],
 sol:"pivot_longer(readings, cols = s1:s3,\n             names_to = \"sensor\", values_to = \"value\")",
 exp:"Long ('tidy') format — one observation per row — is what ggplot2 and dplyr are built around."},

{id:"ar15", lang:"r", topic:"dplyr Pipelines", diff:2, title:"Top 3 earners per department",
 scenario:"`emp` has `dept`, `name`, `salary`. Leadership wants each department's three highest-paid people.",
 task:"Using dplyr, group by dept and take the top 3 rows by salary.",
 req:[{re:/group_by\(dept\)/i, hint:"group_by(dept)"},
      {re:/slice_max\(|arrange\(/i, hint:"slice_max(salary, n = 3) (or arrange then slice)"},
      {re:/salary/i, hint:"rank on salary"},
      {re:/n=3|slice\(1:3\)|head\(3\)/i, hint:"keep 3 per group"}],
 sol:"emp %>% group_by(dept) %>% slice_max(salary, n = 3)",
 exp:"slice_max() picks the top-n rows per group — the tidy replacement for arrange(desc(x)) %>% slice(1:3)."},

{id:"ar16", lang:"r", topic:"dplyr Pipelines", diff:1, title:"Conversion rate column",
 scenario:"`ads` has `clicks` and `impressions` per campaign.",
 task:"Add a column `ctr` equal to clicks divided by impressions (dplyr).",
 req:[{re:/mutate\(/i, hint:"use mutate()"},
      {re:/ctr=clicks\/impressions/i, hint:"ctr = clicks / impressions"}],
 sol:"ads %>% mutate(ctr = clicks / impressions)",
 exp:"mutate() computes columns elementwise across rows — no loop needed."},

{id:"ar17", lang:"r", topic:"Strings & Regex", diff:2, title:"Find the SKU rows",
 scenario:"An inventory frame `inv` has a `code` column; real products start with `SKU-`, junk rows don't.",
 task:"Keep only the rows whose code starts with \"SKU-\" (logical regex matching + indexing).",
 req:[{re:/grepl\(/i, hint:"grepl() gives the logical mask"},
      {re:/\^SKU-/i, hint:'anchor the pattern: "^SKU-"'},
      {re:/inv\[|filter\(/i, hint:"index rows with the mask (or dplyr filter)"}],
 sol:"inv[grepl(\"^SKU-\", inv$code), ]",
 exp:"grepl() returns TRUE/FALSE per element — drop it straight into row indexing. The ^ anchors the match to the string start."},

{id:"ar18", lang:"r", topic:"Dates", diff:2, title:"This year's orders",
 scenario:"`orders$date` was read in as character strings like \"2026-03-14\".",
 task:"Convert the column to Date type, then keep rows on or after \"2026-01-01\".",
 req:[{re:/as\.Date\(/i, hint:"convert with as.Date()"},
      {re:/orders\$date/i, hint:"work on orders$date"},
      {re:/>="2026-01-01"/i, hint:'compare against "2026-01-01"'}],
 sol:"orders$date <- as.Date(orders$date)\norders[orders$date >= \"2026-01-01\", ]",
 exp:"Once a column is Date class, comparison and arithmetic just work; strings would sort lexically and betray you eventually."},

{id:"ar19", lang:"r", topic:"Data Cleaning", diff:2, title:"Trim the outliers",
 scenario:"Reaction-time vector `rt` has fat-fingered entries; the lab's rule is drop anything beyond 3 SDs of the mean.",
 task:"Keep only values within 3 standard deviations (z-score idiom with scale() or mean/sd).",
 req:[{re:/abs\(/i, hint:"absolute z-scores with abs()"},
      {re:/scale\(rt\)|\(rt-mean\(rt\)\)\/sd\(rt\)/i, hint:"z = scale(rt) or (rt-mean(rt))/sd(rt)"},
      {re:/<3/i, hint:"threshold at < 3"},
      {re:/rt\[/i, hint:"index rt with the logical mask"}],
 sol:"rt[abs(scale(rt)) < 3]",
 exp:"scale() centers and divides by SD; the mask keeps mild values. (For skewed data, prefer IQR fences over z-scores.)"},

{id:"ar20", lang:"r", topic:"Simulation", diff:2, title:"Hold out a test set",
 scenario:"You must split data frame `df` (n rows) into 80% train / 20% test, reproducibly.",
 task:"Set seed 1, sample 80% of row indices into `idx`, then build `train` from those rows.",
 req:[{re:/set\.seed\(1\)/i, hint:"set.seed(1) first"},
      {re:/sample\(/i, hint:"sample row indices"},
      {re:/0\.8\*nrow\(df\)|nrow\(df\)\*0\.8/i, hint:"size = 0.8 * nrow(df)"},
      {re:/df\[idx,\]/i, hint:"train <- df[idx, ]"}],
 sol:"set.seed(1)\nidx <- sample(nrow(df), 0.8 * nrow(df))\ntrain <- df[idx, ]\ntest  <- df[-idx, ]",
 exp:"sample(n, k) picks row positions; negative indexing (-idx) gives the complementary test set for free."},

{id:"ar21", lang:"r", topic:"Hypothesis Testing", diff:3, title:"A/B conversion test",
 scenario:"Variant A converted 120 of 2400 visitors; variant B converted 165 of 2500. Product wants to know if B is really better.",
 task:"Run a two-sample proportion test on these counts.",
 req:[{re:/prop\.test\(/i, hint:"use prop.test()"},
      {re:/c\(120,165\)/i, hint:"successes: c(120, 165)"},
      {re:/c\(2400,2500\)/i, hint:"trials: c(2400, 2500)"}],
 sol:"prop.test(c(120, 165), c(2400, 2500))",
 exp:"prop.test(successes, trials) compares proportions with a chi-square approximation — the workhorse of A/B analysis in R."},

{id:"ar22", lang:"r", topic:"Simulation", diff:2, title:"Call-center staffing",
 scenario:"Calls arrive at about 12 per hour. Operations wants a feel for hour-to-hour variation.",
 task:"Simulate the number of calls in each of 168 hours (one week) from a Poisson with mean 12.",
 req:[{re:/rpois\(/i, hint:"use rpois()"},
      {re:/168/, hint:"168 hours"},
      {re:/(lambda=)?12/i, hint:"lambda = 12"}],
 sol:"rpois(168, lambda = 12)",
 exp:"Counts of independent arrivals in fixed windows are the textbook Poisson setting; rpois(n, lambda) simulates them."},

{id:"ar23", lang:"r", topic:"Summary Statistics", diff:1, title:"SLA percentile",
 scenario:"Support tracks resolution times `hrs`; the SLA is written against the 90th percentile.",
 task:"Compute the 90th percentile of `hrs`.",
 req:[{re:/quantile\(/i, hint:"use quantile()"},
      {re:/hrs/i, hint:"on hrs"},
      {re:/0?\.9/, hint:"probs = 0.9"}],
 sol:"quantile(hrs, 0.9)",
 exp:"quantile(x, p) returns empirical percentiles; feed it a vector of probs for several at once."},

{id:"ar24", lang:"r", topic:"Hypothesis Testing", diff:2, title:"CI for average order value",
 scenario:"Finance wants a 95% confidence interval for mean order value from the sample `aov`.",
 task:"Get the interval using a one-sample t procedure (extract the conf.int element).",
 req:[{re:/t\.test\(aov\)/i, hint:"t.test(aov) does the work"},
      {re:/\$conf\.int/i, hint:"extract $conf.int"}],
 sol:"t.test(aov)$conf.int",
 exp:"A one-sample t.test() carries the 95% CI in its conf.int element — no hand-rolled formula needed."},

{id:"ar25", lang:"r", topic:"Apply Family", diff:2, title:"Worst-case sensor per row",
 scenario:"Matrix `X` holds one row per machine, one column per sensor reading; alarms key off each machine's max reading.",
 task:"Compute the maximum of each row using apply.",
 req:[{re:/apply\(X/i, hint:"apply over X"},
      {re:/,1,/i, hint:"MARGIN = 1 (rows)"},
      {re:/max/i, hint:"FUN = max"}],
 sol:"apply(X, 1, max)",
 exp:"MARGIN = 1 walks rows. (For sums/means use rowSums/rowMeans instead — but there's no rowMax, so apply earns its keep here.)"},

{id:"ar26", lang:"r", topic:"Apply Family", diff:2, title:"Column type audit",
 scenario:"A freshly imported data frame `raw` has suspicious column types.",
 task:"Return the class of every column in one line with an apply-family call (simplified output).",
 req:[{re:/sapply\(raw,class\)/i, hint:"sapply(raw, class)"}],
 sol:"sapply(raw, class)",
 exp:"A data frame is a list of columns, so sapply maps over columns and simplifies to a named character vector."},

{id:"ar27", lang:"r", topic:"Factors", diff:1, title:"Response tally",
 scenario:"Survey vector `resp` holds answers like \"agree\", \"neutral\", \"disagree\".",
 task:"Produce a frequency count of each response.",
 req:[{re:/table\(resp\)/i, hint:"table(resp)"}],
 sol:"table(resp)",
 exp:"table() is R's instant frequency counter — the counterpart of pandas' value_counts()."},

{id:"ar28", lang:"r", topic:"Strings & Regex", diff:1, title:"Build sample IDs",
 scenario:"A lab needs IDs like \"A-001\" from vectors `batch` (\"A\") and `num` (\"001\").",
 task:"Create the IDs by concatenating with a dash separator (value-returning function).",
 req:[{re:/paste0?\(/i, hint:"use paste()/paste0()"},
      {re:/batch/i, hint:"include batch"},
      {re:/num/i, hint:"include num"},
      {re:/sep="-"|,"-",/i, hint:'dash separator'}],
 sol:"paste(batch, num, sep = \"-\")",
 exp:"paste() vectorizes over its arguments and returns the strings — cat() would only print them."},

{id:"ar29", lang:"r", topic:"Transforming", diff:1, title:"Pass/fail recode",
 scenario:"Registrar rule: `score >= 60` passes.",
 task:"Create a vector `result` that is \"pass\" or \"fail\" for each element of `score`, vectorized (no loop).",
 req:[{re:/result(<-|=)/i, hint:"assign to result"},
      {re:/ifelse\(/i, hint:"use ifelse()"},
      {re:/score>=60/i, hint:"condition: score >= 60"},
      {re:/"pass"/i, hint:'"pass" when TRUE'},
      {re:/"fail"/i, hint:'"fail" when FALSE'}],
 sol:"result <- ifelse(score >= 60, \"pass\", \"fail\")",
 exp:"ifelse() is the vectorized branch — one call handles the whole vector, unlike if/else which tests a single condition."},

{id:"ar30", lang:"r", topic:"Transforming", diff:2, title:"Age brackets",
 scenario:"Marketing wants customers bucketed into 18–34, 35–54, 55+.",
 task:"Bin the numeric vector `age` at breaks 18, 35, 55, Inf using cut().",
 req:[{re:/cut\(age/i, hint:"cut(age, ...)"},
      {re:/breaks=c\(18,35,55,Inf\)|c\(18,35,55,Inf\)/i, hint:"breaks = c(18, 35, 55, Inf)"}],
 sol:"cut(age, breaks = c(18, 35, 55, Inf),\n    labels = c(\"18-34\", \"35-54\", \"55+\"))",
 exp:"cut() converts numeric to ordered factor bins — the standard first step before grouped rates by bracket."},

{id:"ar31", lang:"r", topic:"Data Cleaning", diff:1, title:"De-duplicate signups",
 scenario:"The signup export `su` accidentally contains repeated rows.",
 task:"Return `su` with duplicate rows removed (base R idiom).",
 req:[{re:/duplicated\(/i, hint:"use duplicated()"},
      {re:/!duplicated\(su\)/i, hint:"negate it: !duplicated(su)"},
      {re:/su\[/i, hint:"index rows of su"}],
 sol:"su[!duplicated(su), ]",
 exp:"duplicated() flags second-and-later appearances; the negated mask keeps first occurrences. (dplyr::distinct(su) is the tidy version.)"},

{id:"ar32", lang:"r", topic:"Grouped Summaries", diff:2, title:"Base-R group means",
 scenario:"No tidyverse allowed on this legacy server. `df` has numeric `sales` and grouping column `region`.",
 task:"Compute mean sales by region using base R's aggregate() with formula syntax.",
 req:[{re:/aggregate\(/i, hint:"use aggregate()"},
      {re:/sales~region/i, hint:"formula: sales ~ region"},
      {re:/mean/i, hint:"FUN = mean"}],
 sol:"aggregate(sales ~ region, data = df, FUN = mean)",
 exp:"aggregate(y ~ g, data, FUN) is base R's group_by + summarize in one call, returning a data frame."},

{id:"ar33", lang:"r", topic:"Combining Data", diff:2, title:"Stack the monthly files",
 scenario:"`dfs` is a list of 12 data frames, one per month, all with identical columns.",
 task:"Combine them into one data frame using do.call with rbind.",
 req:[{re:/do\.call\(/i, hint:"use do.call()"},
      {re:/rbind/i, hint:"rbind as the function"},
      {re:/dfs/i, hint:"pass the list dfs"}],
 sol:"do.call(rbind, dfs)",
 exp:"do.call(f, list) calls f with the list elements as arguments — the classic base-R way to flatten a list of frames (dplyr::bind_rows(dfs) is the tidy twin)."},

{id:"ar34", lang:"r", topic:"Hypothesis Testing", diff:3, title:"Skewed satisfaction scores",
 scenario:"Satisfaction ratings for two store layouts, `a` and `b`, are ordinal and skewed — a t-test's normality story is shaky.",
 task:"Compare the two groups with the appropriate nonparametric rank test.",
 req:[{re:/wilcox\.test\(/i, hint:"wilcox.test()"},
      {re:/a/i, hint:"group a"},
      {re:/b/i, hint:"group b"}],
 sol:"wilcox.test(a, b)",
 exp:"The Wilcoxon rank-sum (Mann-Whitney) test compares distributions via ranks — no normality assumption required."},

{id:"ar35", lang:"r", topic:"Hypothesis Testing", diff:2, title:"Check normality first",
 scenario:"Before a t-test on the small sample `x` (n = 18), a reviewer demands a formal normality check.",
 task:"Run the Shapiro-Wilk test on `x`.",
 req:[{re:/shapiro\.test\(x\)/i, hint:"shapiro.test(x)"}],
 sol:"shapiro.test(x)",
 exp:"Shapiro-Wilk tests H0: data are normal. With small n it has little power — pair it with a QQ-plot (qqnorm(x))."},

{id:"ar36", lang:"r", topic:"Regression", diff:3, title:"Report the R-squared",
 scenario:"Your boss wants a single number: how much variance the fitted model `fit` explains.",
 task:"Extract R² from the model summary.",
 req:[{re:/summary\(fit\)/i, hint:"summary(fit) first"},
      {re:/\$r\.squared/i, hint:"then $r.squared"}],
 sol:"summary(fit)$r.squared",
 exp:"summary.lm() returns a list; r.squared is one element. Prefer adj.r.squared when comparing models of different sizes."},

{id:"ar37", lang:"r", topic:"ANOVA", diff:3, title:"Three fertilizers",
 scenario:"An agronomy trial `crop` recorded `yield` under three `treatment` levels. Does mean yield differ anywhere?",
 task:"Fit a one-way ANOVA of yield by treatment, saving to `m`.",
 req:[{re:/m(<-|=)/i, hint:"assign to m"},
      {re:/aov\(/i, hint:"use aov()"},
      {re:/yield~treatment/i, hint:"yield ~ treatment"},
      {re:/data=crop|,crop\)/i, hint:"data = crop"}],
 sol:"m <- aov(yield ~ treatment, data = crop)\nsummary(m)",
 exp:"aov() fits the one-way ANOVA; summary(m) gives the F-test. Follow up significant results with TukeyHSD(m)."},

{id:"ar38", lang:"r", topic:"Regression", diff:2, title:"Eyeball the residuals",
 scenario:"Model `fit` looks good on paper; you want a quick check that residuals are roughly symmetric around zero.",
 task:"Plot a histogram of the model's residuals (extractor function, not $).",
 req:[{re:/hist\(/i, hint:"hist()"},
      {re:/resid(uals)?\(fit\)/i, hint:"resid(fit)"}],
 sol:"hist(resid(fit))",
 exp:"resid() is the safe extractor for residuals. A lopsided or long-tailed histogram is your first hint the model misses structure."},

{id:"ar39", lang:"r", topic:"Monte Carlo", diff:3, title:"Integrate by simulation",
 scenario:"You need ∫₀¹ e^(−x²) dx but there's no closed form.",
 task:"Estimate it by vanilla Monte Carlo with 100000 uniform draws (mean of the integrand).",
 req:[{re:/mean\(/i, hint:"average the integrand values"},
      {re:/exp\(-/i, hint:"integrand exp(-x^2)"},
      {re:/runif\(1e5\)|runif\(100000\)/i, hint:"runif(1e5) draws on (0,1)"}],
 sol:"mean(exp(-runif(1e5)^2))",
 exp:"Vanilla Monte Carlo: E[f(U)] ≈ mean of f at uniform draws (times interval length, here 1). Importance sampling only becomes necessary when you can't sample the base density well."},

{id:"ar40", lang:"r", topic:"Simulation", diff:3, title:"Power by simulation",
 scenario:"Before launching an experiment (n = 100 per arm, true rates 0.10 vs 0.13), you simulate one dataset to prototype the analysis.",
 task:"Set seed 7, then draw the two groups' conversion counts as binomials `xa` and `xb`.",
 req:[{re:/set\.seed\(7\)/i, hint:"set.seed(7)"},
      {re:/xa(<-|=)rbinom\(1,100,0?\.10?\)/i, hint:"xa <- rbinom(1, 100, 0.10)"},
      {re:/xb(<-|=)rbinom\(1,100,0?\.13\)/i, hint:"xb <- rbinom(1, 100, 0.13)"}],
 sol:"set.seed(7)\nxa <- rbinom(1, 100, 0.10)\nxb <- rbinom(1, 100, 0.13)",
 exp:"rbinom(n, size, prob) simulates conversion counts; wrap this in replicate() and count rejections to estimate power."},

{id:"ar41", lang:"r", topic:"dplyr Pipelines", diff:3, title:"Spread and center by group",
 scenario:"Ops wants both the mean and standard deviation of `latency` for each `server` in `logs`.",
 task:"One dplyr pipeline: group by server, summarize columns `m = mean(latency)` and `s = sd(latency)`.",
 req:[{re:/group_by\(server\)/i, hint:"group_by(server)"},
      {re:/summari[sz]e\(/i, hint:"summarize both stats"},
      {re:/m=mean\(latency\)/i, hint:"m = mean(latency)"},
      {re:/s=sd\(latency\)/i, hint:"s = sd(latency)"}],
 sol:"logs %>% group_by(server) %>%\n  summarize(m = mean(latency), s = sd(latency))",
 exp:"summarize() takes any number of name = FUN(col) pairs — one pass, one tidy result table."},

{id:"ar42", lang:"r", topic:"dplyr Pipelines", diff:1, title:"Headcount per office",
 scenario:"HR asks: how many employees are in each `office` in `emp`?",
 task:"Count rows per group with dplyr (group_by + summarize with n(), or count()).",
 req:[{re:/count\(emp,office\)|group_by\(office\)/i, hint:"count(emp, office) or group_by(office)"},
      {re:/n\(\)|count\(/i, hint:"n() counts rows per group"}],
 sol:"emp %>% group_by(office) %>% summarize(n = n())\n# or: count(emp, office)",
 exp:"n() inside summarize counts group rows; count() is the one-call shortcut for exactly this."},

{id:"ar43", lang:"r", topic:"Joins", diff:3, title:"Keys with different names",
 scenario:"`invoices` uses column `cust_id` while `customers` uses `id` — same key, different names.",
 task:"Left-join customers onto invoices, mapping the differing key names in `by`.",
 req:[{re:/left_join\(/i, hint:"left_join()"},
      {re:/invoices/i, hint:"invoices on the left"},
      {re:/by=c\("cust_id"="id"\)/i, hint:'by = c("cust_id" = "id")'}],
 sol:"left_join(invoices, customers, by = c(\"cust_id\" = \"id\"))",
 exp:"The named-vector form of by maps left-key = right-key when column names differ."},

{id:"ar44", lang:"r", topic:"Strings & Regex", diff:1, title:"Standardize city names",
 scenario:"Free-text `city` entries arrive like \"  chicago \" and \"CHICAGO\".",
 task:"Clean the vector: trim surrounding whitespace, then uppercase (nested calls).",
 req:[{re:/toupper\(/i, hint:"toupper()"},
      {re:/trimws\(/i, hint:"trimws()"},
      {re:/city/i, hint:"apply to city"}],
 sol:"toupper(trimws(city))",
 exp:"trimws() strips leading/trailing whitespace; toupper() canonicalizes case — do both before any grouping or joining on text keys."},

{id:"ar45", lang:"r", topic:"Summary Statistics", diff:2, title:"Name the top performer",
 scenario:"`sales` is a numeric vector named by salesperson.",
 task:"Return the NAME of the person with the highest sales (names + which.max).",
 req:[{re:/which\.max\(sales\)/i, hint:"which.max(sales) finds the position"},
      {re:/names\(/i, hint:"wrap with names(...) to get the label"}],
 sol:"names(sales)[which.max(sales)]",
 exp:"which.max() gives the index of the maximum; indexing the names attribute converts position to identity."},

{id:"ar46", lang:"r", topic:"Apply Family", diff:2, title:"Survey composite score",
 scenario:"Data frame `svy` has five Likert items `q1`–`q5`; the instrument's score is each respondent's item mean.",
 task:"Compute the per-row mean of columns q1:q5 with the fast specialized function.",
 req:[{re:/rowMeans\(/i, hint:"rowMeans()"},
      {re:/svy\[/i, hint:"select the q columns, e.g. svy[, c(\"q1\",...)] or svy[,1:5]"}],
 sol:"rowMeans(svy[, c(\"q1\",\"q2\",\"q3\",\"q4\",\"q5\")])",
 exp:"rowMeans() beats apply(X, 1, mean) — specialized C code, and NA handling via na.rm = TRUE."},

{id:"ar47", lang:"r", topic:"Transforming", diff:2, title:"Standardize before modeling",
 scenario:"Features in `X` are on wildly different scales; the regularized model needs standardized inputs.",
 task:"Center and scale every column of `X` in one call.",
 req:[{re:/scale\(X\)/i, hint:"scale(X)"}],
 sol:"scale(X)",
 exp:"scale() subtracts column means and divides by column SDs — same preprocessing sklearn's StandardScaler does."},

{id:"ar48", lang:"r", topic:"Permutation Test", diff:3, title:"Shuffle-test the difference",
 scenario:"Group labels `g` (\"A\"/\"B\") and outcomes `y`. You want a permutation null for the difference in group means.",
 task:"Simulate 1000 permuted differences: shuffle `g` with sample() each time and recompute mean(y[g==\"A\"]) - mean(y[g==\"B\"]) via tapply or direct means, storing in `perm`.",
 req:[{re:/perm(<-|=)replicate\(1000/i, hint:"perm <- replicate(1000, ...)"},
      {re:/sample\(g\)/i, hint:"shuffle labels with sample(g)"},
      {re:/mean\(/i, hint:"difference of group means"}],
 sol:"perm <- replicate(1000, {\n  s <- sample(g)\n  mean(y[s == \"A\"]) - mean(y[s == \"B\"])\n})",
 exp:"Shuffling labels breaks any real association; comparing the observed difference to `perm` gives an assumption-light p-value."},

{id:"ar49", lang:"r", topic:"Time Series", diff:2, title:"Daily log returns",
 scenario:"`price` is a vector of daily closing prices for a stock.",
 task:"Compute the vector of daily log returns (difference of logs).",
 req:[{re:/diff\(/i, hint:"diff()"},
      {re:/log\(price\)/i, hint:"log(price) inside"}],
 sol:"diff(log(price))",
 exp:"diff(log(p)) = log(p_t / p_{t-1}) — log returns add across time, which makes them the analyst's default."},

{id:"ar50", lang:"r", topic:"Numerical Methods", diff:3, title:"Break-even point",
 scenario:"Profit as a function of price is coded as `f`; it crosses zero somewhere between 0 and 100.",
 task:"Find the root of `f` on the interval (0, 100).",
 req:[{re:/uniroot\(f/i, hint:"uniroot(f, ...)"},
      {re:/c\(0,100\)|lower=0|interval/i, hint:"interval c(0, 100)"}],
 sol:"uniroot(f, c(0, 100))",
 exp:"uniroot() brackets a single root numerically — R's cousin of scipy.optimize.brentq. (polyroot() is the special-case tool for polynomials.)"},

/* ========================== PYTHON APPLIED (50) ========================== */

{id:"ap01", lang:"python", topic:"Grouped Summaries", diff:1, title:"Salary by education",
 scenario:"HR gives you a DataFrame `emp` with columns `Salary` and `Education`.",
 task:"Compute the average salary per education level with a groupby.",
 req:[{re:/\.groupby\("Education"\)/i, hint:'groupby("Education")'},
      {re:/\["Salary"\]/i, hint:"select the Salary column"},
      {re:/\.mean\(\)/i, hint:"aggregate with .mean()"}],
 sol:"emp.groupby(\"Education\")[\"Salary\"].mean()",
 exp:"groupby → select → aggregate is pandas' split-apply-combine — the same job R's tapply does."},

{id:"ap02", lang:"python", topic:"Hypothesis Testing", diff:2, title:"Drug trial t-test",
 scenario:"Arrays `drug` and `placebo` hold blood-pressure reductions per patient (scipy.stats imported as `stats`).",
 task:"Test whether mean reduction differs between the groups, storing the result in `res`.",
 req:[{re:/res=/i, hint:"assign to res"},
      {re:/stats\.ttest_ind\(/i, hint:"stats.ttest_ind()"},
      {re:/drug/i, hint:"pass drug"},
      {re:/placebo/i, hint:"pass placebo"}],
 sol:"res = stats.ttest_ind(drug, placebo)",
 exp:"ttest_ind() returns (statistic, pvalue). Add equal_var=False for Welch's version — which is R's t.test() default."},

{id:"ap03", lang:"python", topic:"Pandas Pipelines", diff:2, title:"Revenue by category",
 scenario:"`orders` has `category`, `price`, and `qty`; finance wants revenue per category.",
 task:"Create a `revenue` column (price × qty), then sum it per category with groupby.",
 req:[{re:/orders\["revenue"\]=|assign\(revenue=/i, hint:'create orders["revenue"]'},
      {re:/price"?\]?\*(orders\[)?"?qty/i, hint:"price * qty"},
      {re:/groupby\("category"\)/i, hint:'groupby("category")'},
      {re:/\["revenue"\]\.sum\(\)|\.sum\(\)/i, hint:"sum revenue per group"}],
 sol:"orders[\"revenue\"] = orders[\"price\"] * orders[\"qty\"]\norders.groupby(\"category\")[\"revenue\"].sum()",
 exp:"Column arithmetic is vectorized — no loops — and groupby handles the aggregation, mirroring dplyr's mutate + group_by + summarize."},

{id:"ap04", lang:"python", topic:"Data Cleaning", diff:1, title:"Missing-data audit",
 scenario:"Survey DataFrame `sv` arrived messy; you owe the team a per-column missingness report.",
 task:"Count missing values in each column of `sv` in one line.",
 req:[{re:/\.isna\(\)|\.isnull\(\)/i, hint:".isna()"},
      {re:/\.sum\(\)/i, hint:"chain .sum()"}],
 sol:"sv.isna().sum()",
 exp:"isna() marks missing cells; sum() counts True down each column. The single most-typed data-audit line in pandas."},

{id:"ap05", lang:"python", topic:"Bootstrap", diff:3, title:"Bootstrap CI for the median",
 scenario:"Household incomes `inc` (NumPy array) are heavily skewed; normal-theory intervals won't fly.",
 task:"Build 1000 bootstrap medians (resample with replacement at full size) in `boots`, then take the 2.5 and 97.5 percentiles.",
 req:[{re:/np\.median\(/i, hint:"np.median of each resample"},
      {re:/np\.random\.choice\(inc/i, hint:"resample with np.random.choice"},
      {re:/replace=True/i, hint:"replace=True"},
      {re:/range\(1000\)|size=\(?1000/i, hint:"1000 resamples"},
      {re:/np\.percentile\(/i, hint:"np.percentile(boots, [2.5, 97.5])"}],
 sol:"boots = [np.median(np.random.choice(inc, size=len(inc), replace=True))\n         for _ in range(1000)]\nnp.percentile(boots, [2.5, 97.5])",
 exp:"Percentile bootstrap: resample, recompute, read the interval from the empirical distribution — no formula for the median's SE needed."},

{id:"ap06", lang:"python", topic:"Simulation", diff:2, title:"CLT in action",
 scenario:"You want to demo that means of skewed data look normal.",
 task:"Simulate 1000 sample means, each from 40 draws of an Exponential distribution, into `m` (NumPy one-liner encouraged).",
 req:[{re:/m=/i, hint:"assign to m"},
      {re:/np\.random\.exponential\(/i, hint:"np.random.exponential"},
      {re:/size=\(1000,40\)/i, hint:"draw a (1000, 40) matrix"},
      {re:/\.mean\(axis=1\)|np\.mean\(.*axis=1/i, hint:"mean over axis=1"}],
 sol:"m = np.random.exponential(size=(1000, 40)).mean(axis=1)",
 exp:"One matrix of draws, one axis-wise mean — vectorized simulation with no Python loop. plt.hist(m) shows the CLT's bell."},

{id:"ap07", lang:"python", topic:"Regression", diff:2, title:"House price model",
 scenario:"`homes` has `price`, `sqft`, `beds`; sklearn's LinearRegression is imported.",
 task:"Fit a regression of price on sqft and beds, saving the fitted model as `fit`.",
 req:[{re:/fit=LinearRegression\(\)\.fit\(|fit=LinearRegression\(\)/i, hint:"fit = LinearRegression().fit(...)"},
      {re:/\[\["sqft","beds"\]\]/i, hint:'X = homes[["sqft", "beds"]]'},
      {re:/\["price"\]/i, hint:'y = homes["price"]'}],
 sol:"fit = LinearRegression().fit(homes[[\"sqft\", \"beds\"]], homes[\"price\"])",
 exp:"sklearn wants X as a 2-D frame (double brackets!) and y as a Series — the double-vs-single bracket slip is the classic error here."},

{id:"ap08", lang:"python", topic:"Regression", diff:2, title:"Price a new listing",
 scenario:"With the fitted model `fit`, a client asks about a 2000 sqft, 3-bed home (pandas imported as pd).",
 task:"Predict its price — build a one-row DataFrame with matching column names and call predict.",
 req:[{re:/fit\.predict\(/i, hint:"fit.predict(...)"},
      {re:/pd\.DataFrame\(/i, hint:"wrap the new point in pd.DataFrame"},
      {re:/"sqft":\[?2000/i, hint:"sqft: 2000"},
      {re:/"beds":\[?3/i, hint:"beds: 3"}],
 sol:"fit.predict(pd.DataFrame({\"sqft\": [2000], \"beds\": [3]}))",
 exp:"predict() expects the same feature columns as fit; a dict-of-lists DataFrame is the cleanest one-row carrier."},

{id:"ap09", lang:"python", topic:"Correlation", diff:1, title:"Ad spend vs. sales",
 scenario:"`df` has monthly columns `ad_spend` and `sales`.",
 task:"Compute the Pearson correlation between the two columns (Series .corr method).",
 req:[{re:/\["ad_spend"\]/i, hint:"select ad_spend"},
      {re:/\.corr\(/i, hint:".corr()"},
      {re:/\["sales"\]/i, hint:"against sales"}],
 sol:"df[\"ad_spend\"].corr(df[\"sales\"])",
 exp:"Series.corr(other) gives one coefficient; df.corr() gives the whole matrix. Correlation is a first look, not proof of causation."},

{id:"ap10", lang:"python", topic:"Hypothesis Testing", diff:3, title:"Churn vs. plan type",
 scenario:"`cust` has categorical columns `plan` and `churned` (scipy.stats as `stats`, pandas as `pd`).",
 task:"Test independence: build the cross-tab and run the chi-square contingency test.",
 req:[{re:/pd\.crosstab\(/i, hint:"pd.crosstab(...)"},
      {re:/cust\["plan"\]/i, hint:'cust["plan"]'},
      {re:/cust\["churned"\]/i, hint:'cust["churned"]'},
      {re:/stats\.chi2_contingency\(/i, hint:"stats.chi2_contingency"}],
 sol:"tab = pd.crosstab(cust[\"plan\"], cust[\"churned\"])\nstats.chi2_contingency(tab)",
 exp:"crosstab builds the contingency table; chi2_contingency returns (stat, p, dof, expected) — same inference as R's chisq.test(table(...))."},

{id:"ap11", lang:"python", topic:"PCA & Multivariate", diff:3, title:"Compress sensor features",
 scenario:"Matrix `X` holds 40 correlated sensor columns (StandardScaler and PCA imported from sklearn).",
 task:"Standardize X, fit a PCA on the scaled data (save as `p`), then access the proportion of variance per component.",
 req:[{re:/StandardScaler\(\)\.fit_transform\(X\)/i, hint:"StandardScaler().fit_transform(X)"},
      {re:/p=PCA\(\)\.fit\(|p=PCA\(/i, hint:"p = PCA().fit(...)"},
      {re:/p\.explained_variance_ratio_/i, hint:"p.explained_variance_ratio_"}],
 sol:"Xs = StandardScaler().fit_transform(X)\np = PCA().fit(Xs)\np.explained_variance_ratio_",
 exp:"Scale first (PCA chases variance, so units matter), then read explained_variance_ratio_ and cut at the elbow of its cumulative curve."},

{id:"ap12", lang:"python", topic:"Joins", diff:2, title:"Attach orders to customers",
 scenario:"`customers` (one row per `id`) and `orders` (many per `id`) are separate DataFrames; keep every customer.",
 task:"Left-merge orders onto customers by `id`.",
 req:[{re:/pd\.merge\(customers,orders|customers\.merge\(orders/i, hint:"merge customers (left) with orders"},
      {re:/on="id"/i, hint:'on="id"'},
      {re:/how="left"/i, hint:'how="left"'}],
 sol:"pd.merge(customers, orders, on=\"id\", how=\"left\")",
 exp:"how=\"left\" keeps all left-table rows with NaN where no order matches — merge defaults to inner, which would silently drop customers."},

{id:"ap13", lang:"python", topic:"Reshaping", diff:2, title:"Gradebook to wide",
 scenario:"Long gradebook `gb` has `Student`, `Assignment`, `Score` — the registrar wants one row per student.",
 task:"Pivot `gb` wide: students as the index, assignments as columns, scores as values.",
 req:[{re:/\.pivot\(/i, hint:".pivot(...)"},
      {re:/index="Student"/i, hint:'index="Student"'},
      {re:/columns="Assignment"/i, hint:'columns="Assignment"'},
      {re:/values="Score"/i, hint:'values="Score"'}],
 sol:"gb.pivot(index=\"Student\", columns=\"Assignment\", values=\"Score\")",
 exp:"The exact flashcard scenario — DataFrame.pivot() is pandas' pivot_wider. Use pivot_table() instead when (Student, Assignment) pairs can repeat."},

{id:"ap14", lang:"python", topic:"Reshaping", diff:2, title:"Sensors to long",
 scenario:"`readings` is wide: `time` plus sensor columns `s1`, `s2`, `s3`. Your plotting library wants long data.",
 task:"Melt `readings` with time as the identifier and the sensors as value_vars.",
 req:[{re:/\.melt\(|pd\.melt\(/i, hint:"melt()"},
      {re:/id_vars="time"|id_vars=\["time"\]/i, hint:'id_vars="time"'},
      {re:/value_vars=\["s1","s2","s3"\]/i, hint:'value_vars=["s1","s2","s3"]'}],
 sol:"readings.melt(id_vars=\"time\", value_vars=[\"s1\", \"s2\", \"s3\"])",
 exp:"melt() stacks wide columns into variable/value pairs — pandas' pivot_longer."},

{id:"ap15", lang:"python", topic:"Pandas Pipelines", diff:2, title:"Top 3 earners per department",
 scenario:"`emp` has `dept`, `name`, `salary`.",
 task:"Return each department's three highest-paid rows (sort first, then take per-group heads).",
 req:[{re:/sort_values\("salary",ascending=False\)/i, hint:'sort_values("salary", ascending=False)'},
      {re:/groupby\("dept"\)/i, hint:'groupby("dept")'},
      {re:/\.head\(3\)/i, hint:".head(3) per group"}],
 sol:"emp.sort_values(\"salary\", ascending=False).groupby(\"dept\").head(3)",
 exp:"groupby().head(n) after a sort keeps top-n rows per group with all original columns intact — the pandas twin of slice_max."},

{id:"ap16", lang:"python", topic:"Pandas Pipelines", diff:1, title:"Conversion rate column",
 scenario:"`ads` has `clicks` and `impressions` per campaign.",
 task:"Add a `ctr` column equal to clicks divided by impressions.",
 req:[{re:/ads\["ctr"\]=/i, hint:'ads["ctr"] = ...'},
      {re:/ads\["clicks"\]\/ads\["impressions"\]/i, hint:'ads["clicks"] / ads["impressions"]'}],
 sol:"ads[\"ctr\"] = ads[\"clicks\"] / ads[\"impressions\"]",
 exp:"Column division is elementwise and vectorized — the everyday way to derive rates in pandas."},

{id:"ap17", lang:"python", topic:"Strings & Regex", diff:2, title:"Find the SKU rows",
 scenario:"Inventory frame `inv` has a `code` column; real products start with `SKU-`.",
 task:"Keep only rows whose code starts with \"SKU-\" using the .str regex accessor.",
 req:[{re:/inv\[/i, hint:"boolean-mask the frame"},
      {re:/\.str\.(match|startswith|contains)\(/i, hint:".str.match / .str.startswith"},
      {re:/SKU-/i, hint:'pattern "SKU-"'}],
 sol:"inv[inv[\"code\"].str.match(\"^SKU-\")]",
 exp:"The .str accessor vectorizes string ops over a column; .str.startswith(\"SKU-\") works too (no regex needed)."},

{id:"ap18", lang:"python", topic:"Dates", diff:2, title:"This year's orders",
 scenario:"`orders[\"date\"]` was read as plain strings like \"2026-03-14\".",
 task:"Convert the column to datetime with pd.to_datetime, then keep rows on or after \"2026-01-01\".",
 req:[{re:/pd\.to_datetime\(/i, hint:"pd.to_datetime(...)"},
      {re:/orders\["date"\]=/i, hint:"reassign the converted column"},
      {re:/>="2026-01-01"/i, hint:'filter >= "2026-01-01"'}],
 sol:"orders[\"date\"] = pd.to_datetime(orders[\"date\"])\norders[orders[\"date\"] >= \"2026-01-01\"]",
 exp:"Once dtype is datetime64, string comparisons parse automatically and .dt unlocks year/month/day accessors."},

{id:"ap19", lang:"python", topic:"Data Cleaning", diff:2, title:"Trim the outliers",
 scenario:"Reaction times `rt` (NumPy array) contain fat-fingered entries; the rule is drop beyond 3 SDs (scipy.stats as `stats`).",
 task:"Keep only values whose absolute z-score is under 3.",
 req:[{re:/np\.abs\(|abs\(/i, hint:"absolute value"},
      {re:/stats\.zscore\(rt\)|\(rt-rt\.mean\(\)\)\/rt\.std\(\)/i, hint:"stats.zscore(rt)"},
      {re:/<3/i, hint:"threshold < 3"},
      {re:/rt\[/i, hint:"boolean-index rt"}],
 sol:"rt[np.abs(stats.zscore(rt)) < 3]",
 exp:"zscore standardizes; the boolean mask keeps mild values. Prefer IQR fences when the data are skewed."},

{id:"ap20", lang:"python", topic:"Machine Learning Prep", diff:2, title:"Hold out a test set",
 scenario:"You must split features `X` and target `y` 80/20, reproducibly (train_test_split imported).",
 task:"Create X_train, X_test, y_train, y_test with a 20% test size and random_state 1.",
 req:[{re:/train_test_split\(X,y/i, hint:"train_test_split(X, y, ...)"},
      {re:/test_size=0?\.2/i, hint:"test_size=0.2"},
      {re:/random_state=1/i, hint:"random_state=1"}],
 sol:"X_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.2, random_state=1)",
 exp:"random_state pins the shuffle so results reproduce — the sklearn equivalent of set.seed before sample()."},

{id:"ap21", lang:"python", topic:"Hypothesis Testing", diff:3, title:"A/B conversion test",
 scenario:"Variant A: 120 of 2400 converted. Variant B: 165 of 2500. (statsmodels' proportions_ztest is imported.)",
 task:"Run a two-sample proportions z-test on these counts.",
 req:[{re:/proportions_ztest\(/i, hint:"proportions_ztest(...)"},
      {re:/\[120,165\]/i, hint:"counts [120, 165]"},
      {re:/\[2400,2500\]/i, hint:"nobs [2400, 2500]"}],
 sol:"proportions_ztest([120, 165], [2400, 2500])",
 exp:"proportions_ztest(count, nobs) is the Python analogue of R's prop.test — the daily bread of A/B analysis."},

{id:"ap22", lang:"python", topic:"Simulation", diff:2, title:"Call-center staffing",
 scenario:"Calls arrive at roughly 12 per hour; ops wants a simulated week.",
 task:"Simulate calls per hour for 168 hours from a Poisson with mean 12.",
 req:[{re:/np\.random\.poisson\(/i, hint:"np.random.poisson"},
      {re:/12/, hint:"lam=12"},
      {re:/168/, hint:"size=168"}],
 sol:"np.random.poisson(12, size=168)",
 exp:"poisson(lam, size) simulates independent counts — R's rpois with the argument order flipped."},

{id:"ap23", lang:"python", topic:"Summary Statistics", diff:1, title:"SLA percentile",
 scenario:"Resolution times `hrs` (array); the SLA is defined at the 90th percentile.",
 task:"Compute the 90th percentile of `hrs` with NumPy.",
 req:[{re:/np\.percentile\(hrs,90\)|np\.quantile\(hrs,0?\.9\)/i, hint:"np.percentile(hrs, 90)"}],
 sol:"np.percentile(hrs, 90)",
 exp:"Careful with scales: np.percentile takes 0-100 while np.quantile (and R's quantile) take 0-1."},

{id:"ap24", lang:"python", topic:"Hypothesis Testing", diff:3, title:"CI for average order value",
 scenario:"Sample `aov` (array) — finance wants a 95% confidence interval for the mean (scipy.stats as `stats`).",
 task:"Compute it with stats.t.interval, using the standard error from stats.sem.",
 req:[{re:/stats\.t\.interval\(/i, hint:"stats.t.interval(...)"},
      {re:/0?\.95/, hint:"confidence 0.95"},
      {re:/len\(aov\)-1|df=/i, hint:"df = len(aov) - 1"},
      {re:/stats\.sem\(aov\)/i, hint:"scale = stats.sem(aov)"}],
 sol:"stats.t.interval(0.95, df=len(aov)-1,\n                 loc=aov.mean(), scale=stats.sem(aov))",
 exp:"t.interval(conf, df, loc, scale) assembles mean ± t* × SE — what R's t.test(x)$conf.int hands you in one step."},

{id:"ap25", lang:"python", topic:"Row-wise Ops", diff:1, title:"Worst-case sensor per row",
 scenario:"`X` is a DataFrame: one row per machine, one column per sensor.",
 task:"Compute each machine's maximum reading (row-wise max).",
 req:[{re:/\.max\(axis=1\)/i, hint:".max(axis=1)"}],
 sol:"X.max(axis=1)",
 exp:"axis=1 means across columns (per row) — the eternal pandas axis gotcha; axis=0 would collapse rows per column."},

{id:"ap26", lang:"python", topic:"Data Inspection", diff:1, title:"Column type audit",
 scenario:"Freshly imported DataFrame `raw` has suspicious column types.",
 task:"Display the dtype of every column.",
 req:[{re:/raw\.dtypes/i, hint:"raw.dtypes"}],
 sol:"raw.dtypes",
 exp:"dtypes (attribute — no parens) is the first sanity check after any read_csv; 'object' columns often hide numbers-as-strings."},

{id:"ap27", lang:"python", topic:"Transforming", diff:2, title:"Make it categorical",
 scenario:"`df[\"plan\"]` holds a few repeated strings; memory and groupbys would both benefit from a proper categorical type.",
 task:"Convert the column to pandas' category dtype (reassign it).",
 req:[{re:/df\["plan"\]=/i, hint:"reassign the column"},
      {re:/\.astype\("category"\)/i, hint:'.astype("category")'}],
 sol:"df[\"plan\"] = df[\"plan\"].astype(\"category\")",
 exp:"Category dtype ≈ R's factor: stored as codes + levels — smaller and faster for repeated labels."},

{id:"ap28", lang:"python", topic:"Strings & Regex", diff:1, title:"Build sample IDs",
 scenario:"A lab needs IDs like \"A-001\" from string columns `batch` and `num` of `df`.",
 task:"Create the combined IDs by concatenating the columns with a dash.",
 req:[{re:/df\["batch"\]/i, hint:'df["batch"]'},
      {re:/\+"-"\+/i, hint:'+ "-" +'},
      {re:/df\["num"\]/i, hint:'df["num"]'}],
 sol:"df[\"batch\"] + \"-\" + df[\"num\"]",
 exp:"String columns concatenate elementwise with + — pandas' vectorized answer to R's paste(sep=\"-\")."},

{id:"ap29", lang:"python", topic:"Transforming", diff:1, title:"Pass/fail recode",
 scenario:"Registrar rule: `score >= 60` passes. `score` is a NumPy array or Series.",
 task:"Create `result` holding \"pass\" or \"fail\" for each element, vectorized.",
 req:[{re:/result=/i, hint:"assign to result"},
      {re:/np\.where\(/i, hint:"np.where(cond, a, b)"},
      {re:/score>=60/i, hint:"score >= 60"},
      {re:/"pass"/i, hint:'"pass"'},
      {re:/"fail"/i, hint:'"fail"'}],
 sol:"result = np.where(score >= 60, \"pass\", \"fail\")",
 exp:"np.where is the vectorized ternary — the exact twin of R's ifelse()."},

{id:"ap30", lang:"python", topic:"Transforming", diff:2, title:"Age brackets",
 scenario:"Marketing wants customers bucketed 18-34, 35-54, 55+.",
 task:"Bin the `age` column with pd.cut at edges 18, 35, 55, and infinity.",
 req:[{re:/pd\.cut\(/i, hint:"pd.cut(...)"},
      {re:/age/i, hint:"bin age"},
      {re:/bins=\[18,35,55,np\.inf\]|\[18,35,55,np\.inf\]/i, hint:"bins=[18, 35, 55, np.inf]"}],
 sol:"pd.cut(df[\"age\"], bins=[18, 35, 55, np.inf],\n       labels=[\"18-34\", \"35-54\", \"55+\"])",
 exp:"pd.cut mirrors R's cut(): numeric in, ordered categorical out — ready for grouped rates per bracket."},

{id:"ap31", lang:"python", topic:"Data Cleaning", diff:1, title:"De-duplicate signups",
 scenario:"The signup export `su` contains repeated rows.",
 task:"Return `su` with duplicate rows dropped.",
 req:[{re:/su\.drop_duplicates\(\)/i, hint:"su.drop_duplicates()"}],
 sol:"su.drop_duplicates()",
 exp:"drop_duplicates() keeps first occurrences by default; subset= restricts which columns define 'duplicate'."},

{id:"ap32", lang:"python", topic:"Reshaping", diff:3, title:"Pivot with duplicates",
 scenario:"Sales frame `s` has `region`, `product`, `amount` — with MANY rows per (region, product), so .pivot() raises an error.",
 task:"Build a region × product table of mean amounts using the pivot method that aggregates.",
 req:[{re:/pivot_table\(/i, hint:"pivot_table(...)"},
      {re:/index="region"/i, hint:'index="region"'},
      {re:/columns="product"/i, hint:'columns="product"'},
      {re:/values="amount"/i, hint:'values="amount"'},
      {re:/aggfunc="mean"|aggfunc=np\.mean/i, hint:'aggfunc="mean"'}],
 sol:"s.pivot_table(index=\"region\", columns=\"product\",\n              values=\"amount\", aggfunc=\"mean\")",
 exp:"pivot() demands unique index/column pairs; pivot_table() aggregates duplicates — that's the whole difference."},

{id:"ap33", lang:"python", topic:"Combining Data", diff:2, title:"Stack the monthly files",
 scenario:"`dfs` is a list of 12 DataFrames with identical columns, one per month.",
 task:"Combine them into one DataFrame with a clean 0..n index.",
 req:[{re:/pd\.concat\(dfs/i, hint:"pd.concat(dfs)"},
      {re:/ignore_index=True/i, hint:"ignore_index=True"}],
 sol:"pd.concat(dfs, ignore_index=True)",
 exp:"concat stacks the list vertically; ignore_index=True rebuilds the row index instead of repeating each file's 0..k."},

{id:"ap34", lang:"python", topic:"Hypothesis Testing", diff:3, title:"Skewed satisfaction scores",
 scenario:"Ordinal, skewed ratings for two store layouts, arrays `a` and `b` (scipy.stats as `stats`).",
 task:"Compare the groups with the appropriate nonparametric rank test.",
 req:[{re:/stats\.mannwhitneyu\(/i, hint:"stats.mannwhitneyu"},
      {re:/a/i, hint:"group a"},
      {re:/b/i, hint:"group b"}],
 sol:"stats.mannwhitneyu(a, b)",
 exp:"Mann-Whitney U ≡ Wilcoxon rank-sum (R's wilcox.test): compares distributions through ranks, no normality assumed."},

{id:"ap35", lang:"python", topic:"Hypothesis Testing", diff:2, title:"Check normality first",
 scenario:"A reviewer demands a formal normality check of the small sample `x` before your t-test (scipy.stats as `stats`).",
 task:"Run the Shapiro-Wilk test on `x`.",
 req:[{re:/stats\.shapiro\(x\)/i, hint:"stats.shapiro(x)"}],
 sol:"stats.shapiro(x)",
 exp:"Same test, same caveats as R's shapiro.test: low power at small n — always pair with a QQ plot."},

{id:"ap36", lang:"python", topic:"Regression", diff:2, title:"Report the R-squared",
 scenario:"Your sklearn model `fit` was trained on features `X` and target `y`; the boss wants one number.",
 task:"Compute the model's R² on that data with the estimator's scoring method.",
 req:[{re:/fit\.score\(X,y\)/i, hint:"fit.score(X, y)"}],
 sol:"fit.score(X, y)",
 exp:"For regressors, .score() returns R². Scoring on training data flatters the model — report test-set R² when you have a split."},

{id:"ap37", lang:"python", topic:"ANOVA", diff:3, title:"Three fertilizers",
 scenario:"Yield arrays `g1`, `g2`, `g3` — one per fertilizer (scipy.stats as `stats`). Does mean yield differ anywhere?",
 task:"Run a one-way ANOVA across the three groups.",
 req:[{re:/stats\.f_oneway\(/i, hint:"stats.f_oneway"},
      {re:/g1,g2,g3/i, hint:"pass all three groups"}],
 sol:"stats.f_oneway(g1, g2, g3)",
 exp:"f_oneway takes each group as a separate argument and returns the F statistic and p-value — R's aov() in one call."},

{id:"ap38", lang:"python", topic:"Regression", diff:2, title:"Eyeball the residuals",
 scenario:"Predictions `pred` and true values `y` are in hand (matplotlib as `plt`).",
 task:"Plot a histogram of the residuals (y minus pred).",
 req:[{re:/plt\.hist\(/i, hint:"plt.hist(...)"},
      {re:/y-pred/i, hint:"residuals = y - pred"}],
 sol:"plt.hist(y - pred)",
 exp:"A residual histogram centered off zero or heavily skewed says the model is systematically missing something."},

{id:"ap39", lang:"python", topic:"Monte Carlo", diff:3, title:"Integrate by simulation",
 scenario:"You need ∫₀¹ e^(−x²) dx numerically.",
 task:"Estimate it by vanilla Monte Carlo: mean of the integrand at 100000 uniform draws.",
 req:[{re:/np\.mean\(|\.mean\(\)/i, hint:"average the integrand values"},
      {re:/np\.exp\(-/i, hint:"integrand np.exp(-u**2)"},
      {re:/np\.random\.uniform\(size=100000\)|np\.random\.uniform\(0,1,100000\)|np\.random\.rand\(100000\)/i, hint:"100000 uniform draws"}],
 sol:"u = np.random.uniform(size=100000)\nnp.mean(np.exp(-u**2))",
 exp:"E[f(U)] over Uniform(0,1) equals the integral — vanilla Monte Carlo needs only draws you CAN sample; importance sampling is for when you can't."},

{id:"ap40", lang:"python", topic:"Simulation", diff:3, title:"Power by simulation",
 scenario:"Prototype an experiment: n = 100 per arm, true conversion rates 0.10 vs 0.13.",
 task:"Seed NumPy with 7, then draw the two arms' conversion counts as binomials `xa` and `xb`.",
 req:[{re:/np\.random\.seed\(7\)/i, hint:"np.random.seed(7)"},
      {re:/xa=np\.random\.binomial\(100,0?\.10?\)/i, hint:"xa = np.random.binomial(100, 0.10)"},
      {re:/xb=np\.random\.binomial\(100,0?\.13\)/i, hint:"xb = np.random.binomial(100, 0.13)"}],
 sol:"np.random.seed(7)\nxa = np.random.binomial(100, 0.10)\nxb = np.random.binomial(100, 0.13)",
 exp:"binomial(n, p) simulates a conversion count; loop it and tally significant tests to estimate power before you launch."},

{id:"ap41", lang:"python", topic:"Grouped Summaries", diff:2, title:"Spread and center by group",
 scenario:"Ops wants both mean and standard deviation of `latency` per `server` in `logs`.",
 task:"One groupby with .agg computing both statistics on the latency column.",
 req:[{re:/groupby\("server"\)/i, hint:'groupby("server")'},
      {re:/\["latency"\]/i, hint:"select latency"},
      {re:/\.agg\(\["mean","std"\]\)/i, hint:'.agg(["mean", "std"])'}],
 sol:"logs.groupby(\"server\")[\"latency\"].agg([\"mean\", \"std\"])",
 exp:"agg with a list of names computes several statistics in one pass — one row per server, one column per stat."},

{id:"ap42", lang:"python", topic:"Grouped Summaries", diff:1, title:"Headcount per office",
 scenario:"HR asks: how many employees per `office` in `emp`?",
 task:"Count rows per office (groupby size or value_counts).",
 req:[{re:/groupby\("office"\)\.size\(\)|\["office"\]\.value_counts\(\)/i, hint:'groupby("office").size() or value_counts()'}],
 sol:"emp.groupby(\"office\").size()\n# or: emp[\"office\"].value_counts()",
 exp:".size() counts all rows per group (including NaN rows); .count() counts non-null values per column — subtly different."},

{id:"ap43", lang:"python", topic:"Joins", diff:3, title:"Keys with different names",
 scenario:"`invoices` uses `cust_id`; `customers` uses `id` — same key, different names.",
 task:"Left-merge customers onto invoices mapping the key names explicitly.",
 req:[{re:/pd\.merge\(invoices,customers|invoices\.merge\(customers/i, hint:"invoices on the left"},
      {re:/left_on="cust_id"/i, hint:'left_on="cust_id"'},
      {re:/right_on="id"/i, hint:'right_on="id"'},
      {re:/how="left"/i, hint:'how="left"'}],
 sol:"pd.merge(invoices, customers,\n         left_on=\"cust_id\", right_on=\"id\", how=\"left\")",
 exp:"left_on / right_on handle mismatched key names — the counterpart of dplyr's by = c(\"cust_id\" = \"id\")."},

{id:"ap44", lang:"python", topic:"Strings & Regex", diff:1, title:"Standardize city names",
 scenario:"Free-text `city` column arrives like \"  chicago \" and \"CHICAGO\".",
 task:"Clean it: strip surrounding whitespace, then uppercase, using chained .str methods.",
 req:[{re:/\.str\.strip\(\)/i, hint:".str.strip()"},
      {re:/\.str\.upper\(\)/i, hint:".str.upper()"}],
 sol:"df[\"city\"].str.strip().str.upper()",
 exp:"Chained .str ops mirror R's toupper(trimws(x)) — always canonicalize text before grouping or joining on it."},

{id:"ap45", lang:"python", topic:"Summary Statistics", diff:2, title:"Name the top performer",
 scenario:"`sales` is a Series indexed by salesperson name.",
 task:"Return the NAME (index label) of the person with the highest sales.",
 req:[{re:/sales\.idxmax\(\)/i, hint:"sales.idxmax()"}],
 sol:"sales.idxmax()",
 exp:"idxmax() returns the index label at the maximum — one call where R needs names(x)[which.max(x)]."},

{id:"ap46", lang:"python", topic:"Row-wise Ops", diff:2, title:"Survey composite score",
 scenario:"`svy` has Likert items `q1`-`q5`; the instrument's score is each respondent's item mean.",
 task:"Compute the per-row mean of just those five columns.",
 req:[{re:/\[\["q1","q2","q3","q4","q5"\]\]/i, hint:'select with double brackets [["q1",...,"q5"]]'},
      {re:/\.mean\(axis=1\)/i, hint:".mean(axis=1)"}],
 sol:"svy[[\"q1\", \"q2\", \"q3\", \"q4\", \"q5\"]].mean(axis=1)",
 exp:"Double brackets keep a DataFrame of the item columns; axis=1 averages across them per respondent — R's rowMeans."},

{id:"ap47", lang:"python", topic:"Transforming", diff:2, title:"Standardize before modeling",
 scenario:"Features in numeric DataFrame `X` sit on wildly different scales.",
 task:"Standardize every column with the vectorized (X − mean) / std idiom.",
 req:[{re:/\(X-X\.mean\(\)\)/i, hint:"(X - X.mean())"},
      {re:/\/X\.std\(\)/i, hint:"/ X.std()"}],
 sol:"(X - X.mean()) / X.std()",
 exp:"Broadcasting subtracts each column's mean and divides by its SD in one expression — R's scale(), or sklearn's StandardScaler in a pipeline."},

{id:"ap48", lang:"python", topic:"Permutation Test", diff:3, title:"Shuffle-test the difference",
 scenario:"Binary labels `g` (array of \"A\"/\"B\") and outcomes `y`. You want a permutation null for the difference in group means.",
 task:"Simulate 1000 permuted differences into list `perm`: shuffle labels with np.random.permutation each round and recompute the mean difference.",
 req:[{re:/perm=\[|perm\.append|for_inrange\(1000\)|foriinrange\(1000\)/i, hint:"loop or comprehension, 1000 rounds"},
      {re:/np\.random\.permutation\(g\)/i, hint:"np.random.permutation(g)"},
      {re:/\.mean\(\)|np\.mean\(/i, hint:"difference of group means"}],
 sol:"perm = []\nfor _ in range(1000):\n    s = np.random.permutation(g)\n    perm.append(y[s == \"A\"].mean() - y[s == \"B\"].mean())",
 exp:"Shuffling labels destroys any true group effect; where the observed difference falls in `perm` is your permutation p-value."},

{id:"ap49", lang:"python", topic:"Time Series", diff:2, title:"Daily log returns",
 scenario:"`price` is a NumPy array of daily closing prices.",
 task:"Compute daily log returns (difference of logs) with NumPy.",
 req:[{re:/np\.diff\(/i, hint:"np.diff(...)"},
      {re:/np\.log\(price\)/i, hint:"np.log(price) inside"}],
 sol:"np.diff(np.log(price))",
 exp:"Identical logic to R's diff(log(price)) — log returns are additive over time, which is why finance defaults to them."},

{id:"ap50", lang:"python", topic:"Numerical Methods", diff:3, title:"Break-even point",
 scenario:"Profit as a function of price is coded as `f`; it crosses zero between 0 and 100 (scipy.optimize imported as `opt`).",
 task:"Find the root of `f` on that bracket with Brent's method.",
 req:[{re:/opt\.brentq\(f|brentq\(f/i, hint:"opt.brentq(f, ...)"},
      {re:/0,100/i, hint:"bracket 0, 100"}],
 sol:"opt.brentq(f, 0, 100)",
 exp:"brentq needs a sign change over [a, b] and then converges fast — SciPy's cousin of R's uniroot()."},

];
